import React from 'react';
import './NoEventsFallback.scss';

const NoEventsFallback = () => {
  return <div className="no-events-fallback">No events available.</div>;
};

export default NoEventsFallback;
